'use strict';

var userWord = prompt("Введите слово на русском");
function wordVer() {
var letter = {a=ё, a=у, a=е, a=ы, a=а, a=о, a=э, a=я, a=и, a=ю};
for (var i = 0; i<userWord.length; i++);
var letterInWord = 0;
if (letter.indexOf(letterInWord) = -1);
letterInWords++
return letterInWords;
};